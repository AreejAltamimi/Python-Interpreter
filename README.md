# Python-Interpreter


Project description: Design and implement an Abstract Syntax Tree (AST) to represent and interpret Python code. It translates Python parameters including actual parameters and formal parameters, implementing recursion, and boolean. 
It handles integer, float, and string values (string concatenation, multiplication, and slicing). It also translates Python if/else constructs, functions, including scope resolution,and return value propogation.
